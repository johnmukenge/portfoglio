import { ReactDOM } from "react-dom";
import App from "./App"; // import the App component
import "./index.css"; // import the stylesheet

ReactDOM.render(<App />, document.getElementById("root")); // render the App component in the DOM